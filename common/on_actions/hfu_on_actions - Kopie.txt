on_actions = {
	#ROOT is new controller #FROM is old controller #FROM.FROM is state ID
	
	on_daily = {
			effect = {
				GER={
				if = {
				limit={has_completed_focus=GER_Mineralölsicherungsplan}
				for_each_scope_loop = {
				array = faction_members_array
				if = {ROOT.faction_members_array
				limit = {
					has_offensive_war_with = FROM
				}
				TLA = {
					add_to_war = {
						targeted_alliance = FROM
						enemy = PREV
						hostility_reason = tlaloc_angry
					}
				}
			}
		}
				ROOT.faction_members_array
					}
					
				}
					country_event={id=surrender.1 days=2}
				}
						
				}
			}}

	
	}
