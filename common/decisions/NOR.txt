NOR_fork = {

		NOR_allies = {

		icon = generic_prepare_civil_war

		available = {
			date > 1948.1.1
			}

		cost = 0
		fire_only_once = yes
		days_remove = 35
		visible = {
			date > 1948.1.1
		}
		
		ai_will_do = {
			factor = 1
		}
		
		complete_effect = {
			
		}
		remove_effect = {
			ENG = { country_event = { id = britain.1113 } }
		}
	}
	
	






	op_fork2 = {

		icon = generic_operation
	
		allowed = {
			tag = NOR
		}
		
		available = {
			tag = NOR
			has_government = democratic
			has_war_with = GER
			ICE = { exists = yes }
			NOR = { has_capitulated = yes }
			is_in_faction_with = NOR
		}

		cost = 50

		ai_will_do = {
			factor = 1
		}
		
		visible = {
			tag = ENG
			has_war_with = GER
			ICE = { exists = yes }
			NOT = { ICE = { is_in_faction_with = ROOT } }
		}
		
		complete_effect = {
			GER = { add_to_faction = ICE }
			ICE = { declare_war_on = { target = ENG type = annex_everything }
			declare_war_on = { target = FRA type = annex_everything }}
			add_popularity = { ideology = democratic popularity = -0.05 }
			ICE = { add_ideas = FRA_no_resistance }
		}
	}
	
		operation_greenland = {

		icon = generic_operation
	
		allowed = {
			tag = NOR
		}
		
		available = {
			tag = NOR
			has_government = democratic
			has_war_with = GER
			ICE = { has_capitulated = yes }
			NOR = { has_capitulated = yes }
			NOR = { has_completed_focus = nor_norway_in_exile }
		}

		cost = 50

		ai_will_do = {
			factor = 1
		}
		
		visible = {
	
		}
		
		complete_effect = {
				NOR = { transfer_state = 101 }
				101 = { add_extra_state_shared_building_slots = 1 }
				NOR = { transfer_state = 337 }
					337 = { add_extra_state_shared_building_slots = 1 }
				}
	}
}