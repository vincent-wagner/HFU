operations = {
	USA_execute_operation_torch = {
		icon = generic_operation
		available = {
			has_war_with = GER
			Date > 1941.1.1
			#has_completed_focus = USA_Torch_Focus
		}
		visible = {
			tag = USA
			Date > 1940.7.1
		}
		ai_will_do = {
			factor = 1
		}
		fire_only_once = yes
		days_remove = 30
		complete_effect = {declare_war_on = {
				target = EFR
				type = annex_everything
			}
			
			
		}
		remove_effect = {
			
			GER = { puppet = EFR }
		}
	}
}