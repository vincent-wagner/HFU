add_other_ncr_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_ncr_strategies (scripted_effect) entered"
  }
  every_country = {
    if = {
      limit = {
        is_caesars_legion_member = yes
      }
      setup_caesars_legion_vs_ncr_relations = yes
      PREV = {
        setup_ncr_vs_caesars_legion_relations = yes
      }
    }
    else_if = {
      limit = {
        OR = {
          is_caesars_legion_member = yes
          is_masters_new_army_member = yes
          is_western_enclave_member = yes
          is_nevada_enclave_member = yes
        }
      }
      #make_enemies = yes
    }
  }
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_ncr_strategies (scripted_effect) exited"
  }
}
remove_other_ncr_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_ncr_strategies (scripted_effect) entered"
  }
  every_country = {
    if = {
      limit = {
        is_caesars_legion_member = yes
      }
      teardown_caesars_legion_vs_ncr_relations = yes
      PREV = {
        teardown_ncr_vs_caesars_legion_relations = yes
      }
    }
    else_if = {
      limit = {
        OR = {
          is_masters_new_army_member = yes
          is_western_enclave_member = yes
          is_nevada_enclave_member = yes
        }
      }
      #unmake_enemies = yes
    }
  }
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_ncr_strategies (scripted_effect) exited"
  }
}

add_other_caesars_legion_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_caesars_legion_strategies (scripted_effect) entered"
  }
  every_country = {
    if = {
      limit = {
        is_ncr_member = yes
      }
      setup_ncr_vs_caesars_legion_relations = yes
      PREV = {
        setup_caesars_legion_vs_ncr_relations = yes
      }
    }
    else_if = {
      limit = {
        OR = {
          is_mormon_alliance_member = yes
        }
      }
      #make_enemies = yes
    }
  }
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_caesars_legion_strategies (scripted_effect) exited"
  }
}
remove_other_caesars_legion_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_caesars_legion_strategies (scripted_effect) entered"
  }
  every_country = {
    if = {
      limit = {
        is_ncr_member = yes
      }
      teardown_caesars_legion_vs_ncr_relations = yes
    }
    else_if = {
      limit = {
        OR = {
          is_mormon_alliance_member = yes
        }
      }
      #unmake_enemies = yes
    }
  }
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_caesars_legion_strategies (scripted_effect) exited"
  }
}

add_other_brotherhood_of_steel_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_brotherhood_of_steel_strategies (scripted_effect) entered"
  }
  every_country = {
    limit = {
      OR = {
        is_masters_new_army_member = yes
        is_northern_league_member = yes
        is_western_enclave_member = yes
        is_nevada_enclave_member = yes
      }
    }
    #make_enemies = yes
  }
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_brotherhood_of_steel_strategies (scripted_effect) exited"
  }
}
remove_other_brotherhood_of_steel_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_brotherhood_of_steel_strategies (scripted_effect) entered"
  }
  every_country = {
    limit = {
      OR = {
        is_masters_new_army_member = yes
        is_northern_league_member = yes
        is_western_enclave_member = yes
        is_nevada_enclave_member = yes
      }
    }
    #unmake_enemies = yes
  }
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_brotherhood_of_steel_strategies (scripted_effect) exited"
  }
}

add_other_mormon_alliance_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_mormon_alliance_strategies (scripted_effect) entered"
  }
  every_country = {
    limit = {
      OR = {
        is_caesars_legion_member = yes
      }
    }
    #make_enemies = yes
  }
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_mormon_alliance_strategies (scripted_effect) exited"
  }
}
remove_other_mormon_alliance_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_mormon_alliance_strategies (scripted_effect) entered"
  }
  every_country = {
    limit = {
      OR = {
        is_caesars_legion_member = yes
      }
    }
    #unmake_enemies = yes
  }
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_mormon_alliance_strategies (scripted_effect) exited"
  }
}

add_other_fantasma_cartel_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_fantasma_cartel_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = 200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_fantasma_cartel_strategies (scripted_effect) exited"
  }
}
remove_other_fantasma_cartel_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_fantasma_cartel_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = -200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_fantasma_cartel_strategies (scripted_effect) exited"
  }
}

add_other_nevada_pact_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_nevada_pact_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = 200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_nevada_pact_strategies (scripted_effect) exited"
  }
}
remove_other_nevada_pact_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_nevada_pact_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = -200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_nevada_pact_strategies (scripted_effect) exited"
  }
}

add_other_mojave_alliance_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_mojave_alliance_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = 200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_mojave_alliance_strategies (scripted_effect) exited"
  }
}
remove_other_mojave_alliance_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_mojave_alliance_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = -200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_mojave_alliance_strategies (scripted_effect) exited"
  }
}

add_other_northern_league_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_northern_league_strategies (scripted_effect) entered"
  }
  every_country = {
    limit = {
      OR = {
        is_brotherhood_of_steel_member = yes
        is_masters_new_army_member = yes
      }
    }
    #make_enemies = yes
  }
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_northern_league_strategies (scripted_effect) exited"
  }
}
remove_other_northern_league_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_northern_league_strategies (scripted_effect) entered"
  }
  every_country = {
    limit = {
      OR = {
        is_brotherhood_of_steel_member = yes
        is_masters_new_army_member = yes
      }
    }
    #unmake_enemies = yes
  }
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_northern_league_strategies (scripted_effect) exited"
  }
}

add_other_phoenix_federation_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_phoenix_federation_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = 200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_phoenix_federation_strategies (scripted_effect) exited"
  }
}
remove_other_phoenix_federation_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_phoenix_federation_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = -200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_phoenix_federation_strategies (scripted_effect) exited"
  }
}

add_other_children_of_the_gate_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_children_of_the_gate_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = 200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_children_of_the_gate_strategies (scripted_effect) exited"
  }
}
remove_other_children_of_the_gate_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_children_of_the_gate_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = -200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_children_of_the_gate_strategies (scripted_effect) exited"
  }
}

add_other_western_enclave_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_western_enclave_strategies (scripted_effect) entered"
  }
  every_country = {
    limit = {
      OR = {
        is_ncr_member = yes
        is_brotherhood_of_steel_member = yes
        is_masters_new_army_member = yes
      }
    }
    #make_enemies = yes
  }
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_western_enclave_strategies (scripted_effect) exited"
  }
}
remove_other_western_enclave_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_western_enclave_strategies (scripted_effect) entered"
  }
  every_country = {
    limit = {
      OR = {
        is_ncr_member = yes
        is_brotherhood_of_steel_member = yes
        is_masters_new_army_member = yes
      }
    }
    #unmake_enemies = yes
  }
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_western_enclave_strategies (scripted_effect) exited"
  }
}

add_other_boneyard_brotherhood_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_boneyard_brotherhood_strategies (scripted_effect) entered"
  }
  every_country = {
    limit = {
      OR = {
        is_masters_new_army_member = yes
        is_northern_league_member = yes
        is_western_enclave_member = yes
        is_nevada_enclave_member = yes
      }
    }
    #make_enemies = yes
  }
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_boneyard_brotherhood_strategies (scripted_effect) exited"
  }
}
remove_other_boneyard_brotherhood_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_boneyard_brotherhood_strategies (scripted_effect) entered"
  }
  every_country = {
    limit = {
      OR = {
        is_masters_new_army_member = yes
        is_northern_league_member = yes
        is_western_enclave_member = yes
        is_nevada_enclave_member = yes
      }
    }
    #unmake_enemies = yes
  }
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_boneyard_brotherhood_strategies (scripted_effect) exited"
  }
}

add_other_nevada_enclave_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_nevada_enclave_strategies (scripted_effect) entered"
  }
  every_country = {
    limit = {
      OR = {
        is_ncr_member = yes
        is_brotherhood_of_steel_member = yes
        is_masters_new_army_member = yes
      }
    }
    #make_enemies = yes
  }
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_nevada_enclave_strategies (scripted_effect) exited"
  }
}
remove_other_nevada_enclave_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_nevada_enclave_strategies (scripted_effect) entered"
  }
  every_country = {
    limit = {
      OR = {
        is_ncr_member = yes
        is_brotherhood_of_steel_member = yes
        is_masters_new_army_member = yes
      }
    }
    #make_enemies = yes
  }
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_nevada_enclave_strategies (scripted_effect) exited"
  }
}

add_other_masters_new_army_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_masters_new_army_strategies (scripted_effect) entered"
  }
  every_country = {
    limit = {
      OR = {
        is_ncr_member = yes
        is_brotherhood_of_steel_member = yes
        is_northern_league_member = yes
        is_western_enclave_member = yes
        is_nevada_enclave_member = yes
      }
    }
    #make_enemies = yes
  }
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_masters_new_army_strategies (scripted_effect) exited"
  }
}
remove_other_masters_new_army_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_masters_new_army_strategies (scripted_effect) entered"
  }
  every_country = {
    limit = {
      OR = {
        is_ncr_member = yes
        is_brotherhood_of_steel_member = yes
        is_northern_league_member = yes
        is_western_enclave_member = yes
        is_nevada_enclave_member = yes
      }
    }
    #unmake_enemies = yes
  }
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_masters_new_army_strategies (scripted_effect) exited"
  }
}

add_other_children_of_mexico_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_children_of_mexico_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = 200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_children_of_mexico_strategies (scripted_effect) exited"
  }
}
remove_other_children_of_mexico_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_children_of_mexico_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = -200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_children_of_mexico_strategies (scripted_effect) exited"
  }
}

add_other_northern_mojave_pact_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_northern_mojave_pact_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = 200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_northern_mojave_pact_strategies (scripted_effect) exited"
  }
}
remove_other_northern_mojave_pact_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_northern_mojave_pact_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = -200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_northern_mojave_pact_strategies (scripted_effect) exited"
  }
}

add_other_jungle_pact_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_jungle_pact_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = 200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_jungle_pact_strategies (scripted_effect) exited"
  }
}
remove_other_jungle_pact_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_jungle_pact_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = -200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_jungle_pact_strategies (scripted_effect) exited"
  }
}

add_other_mesoamerican_alliance_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_mesoamerican_alliance_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = 200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_mesoamerican_alliance_strategies (scripted_effect) exited"
  }
}
remove_other_mesoamerican_alliance_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_mesoamerican_alliance_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = -200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_mesoamerican_alliance_strategies (scripted_effect) exited"
  }
}

add_other_rio_pact_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_rio_pact_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = 200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_rio_pact_strategies (scripted_effect) exited"
  }
}
remove_other_rio_pact_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_rio_pact_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = -200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_rio_pact_strategies (scripted_effect) exited"
  }
}

add_other_great_federation_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_great_federation_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = 200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_great_federation_strategies (scripted_effect) exited"
  }
}
remove_other_great_federation_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_great_federation_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = -200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_great_federation_strategies (scripted_effect) exited"
  }
}

add_other_unbroken_coast_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_unbroken_coast_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = 200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] add_other_unbroken_coast_strategies (scripted_effect) exited"
  }
}
remove_other_unbroken_coast_strategies = {
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_unbroken_coast_strategies (scripted_effect) entered"
  }
  #add_ai_strategy = {
  #  type = alliance
  #  id = PREV
  #  value = -200
  #}
  if = {
    limit = { has_global_flag = debug_logs }
    log = "[GetDateText]: [This.GetName] remove_other_unbroken_coast_strategies (scripted_effect) exited"
  }
}
