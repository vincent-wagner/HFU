scripted_gui = {
	tlaloc_memory_capacity_window = {
		context_type = selected_country_context

		window_name = "tlaloc_memory_capacity_window"
		parent_window_token = selected_country_view

		visible = {
			tag = TLA
			TLA = {
				NOT = {
					has_cosmetic_tag = TLA_RRG
				}
			}
		}
	}

	tlaloc_memory_capacity_topbar_window = {
		context_type = player_context

		window_name = "tlaloc_memory_capacity_topbar_window"
		parent_window_token = top_bar

		visible = {
			tag = TLA
			TLA = {
				NOT = {
					has_cosmetic_tag = TLA_RRG
				}
			}
		}
	}
}
